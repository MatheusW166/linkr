import styled from 'styled-components';

const RepostsStyled = styled.button`
  font-size: 12px;
  display: flex;
  gap: 5px;
  flex-direction: column;
  align-items: center;
  user-select: none;

  svg {
    font-size: 18px;
  }
`;

export default RepostsStyled;
